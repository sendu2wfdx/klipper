# BL24C16F 断电续打源码化分析

## 结论

F009 配置中的 `[bl24c16f]` 不是无关紧要的调试外设，而是创想三维 V57
断电续打链路的非易失存储。原厂通过 T113 上的 Klipper Linux-process MCU
访问 `i2c.1`，总线速率 400 kHz；器件容量为 2 KiB，并使用 `0x50` 至
`0x57` 八个 I2C 从地址，每个地址对应 256 字节。

公版源码线现已重新实现该驱动，保留原厂对外命令和 Python helper API，
同时补上跨 256 字节地址块读取、16 字节页写入拆分、写周期等待、越界保护和
输入数据不被原地修改。原厂正常使用的 4 字节记录在地址和字节序上保持不变。

## 原厂数据布局

- 地址 `0`：当前环形记录位置 `pos`。
- 地址 `1`：断电续打状态。值为 `0xff` 表示未建立有效记录；首次记录时写 `1`，
  打印正常完成或取消时写回 `0xff`。原厂 helper 名称中的 `FirstEnable` 容易产生
  误解，其返回 `True` 实际表示 EEPROM 仍处于擦除/未启用状态。
- 地址 `pos * 8`：G-Code 文件偏移，4 字节小端无符号整数。
- 地址 `pos * 8 + 4`：挤出机基准位置，4 字节 IEEE-754 小端浮点数。
- 每条恢复记录固定 8 字节。原厂 `virtual_sdcard.py` 在满足层高/运动条件后
  周期性更新记录，并在恢复时读取文件偏移和挤出机基准。

## 已还原的原厂接口

调试命令：

- `EEPROM_DEBUG_READ`
- `EEPROM_DEBUG_WRITE_BYTE`
- `EEPROM_DEBUG_WRITE_INT`
- `EEPROM_DEBUG_WRITE_FLOAT`

生产命令：

- `EEPROM_READ`
- `EEPROM_WRITE_BYTE`
- `EEPROM_WRITE_INT`
- `EEPROM_WRITE_FLOAT`
- `EEPROM_IS_FIRST_USED`
- `EEPROM_POS`
- `EEPROM_PRINTER_INFO`

供原厂断电续打逻辑调用的 helper：

- `setEepromDisable()`
- `checkEepromFirstEnable()`
- `eepromReadHeader()`
- `eepromReadBody(pos)`

## 与原厂源码相比的受控修正

原厂驱动只根据首地址选择一个 256 字节地址块，调试读取跨块时可能在器件内部
回卷；写入时会直接在调用者传入的列表首部插入地址字节，并且没有按 EEPROM
页边界拆分。公版实现修正了这些边界行为，但不改变原厂断电续打记录的正常
访问序列，也不改变命令名、参数、字节序和返回数据含义。

## 当前验证

- 八个 I2C 地址创建顺序为 `0x50..0x57`。
- 原厂 11 个命令全部注册，并保留无 `CHIP` 参数的默认调用方式。
- 跨 256 字节地址块读取通过。
- 跨 16 字节页和 256 字节地址块写入拆分通过。
- 断电续打头部、文件偏移和浮点挤出机位置解析通过。
- 2 KiB 空间越界保护通过。
- Windows 公版源码测试总计 `124 passed, 1 skipped`；断电续打与 virtual-SD
  生命周期专项共 19 项。
- T113 上 Linux-process MCU 已能从当前公版源码编译，F009 的 8 KiB 与
  12 KiB 配置测试均使用该 MCU 字典进行验证。

## 状态机第一阶段

公版线已新增独立 `power_loss_recovery` 模块，并只向公版
`virtual_sdcard.py` 加入生命周期事件，避免复制原厂大段互相耦合的核心修改。
当前已实现：

- 原厂 8 字节记录格式、擦除标志和 `255→1` 环形槽位；每个槽位连续更新
  255 次后才轮转，减少 EEPROM 头部和槽位磨损。
- 原厂层号/G1/Z 高度门槛的记录策略，以及 5 秒/15 秒状态落盘节奏。
- G-Code 文件路径的原子写入和续打时的一致性校验。
- `pause_resume/check_continue_print_state` 与
  `pause_resume/cancel_continue_print` 两个原厂 Web API。
- 正常完成和取消时清理；暂停或打印错误时保留恢复记录。

恢复执行源码现已实现，但目标配置仍未开放。规划器从 EEPROM 的精确字节边界
正向重放文件状态，可恢复独立的 XYZ/E 绝对或相对模式、位置、热床/喷嘴温度、
风扇、M204、压力提前量、网床、速度和流量；执行顺序为预热、按保存 Z 建立坐标、
先抬高、XY 归零、等待温度、恢复挤出坐标、XY 回位、Z 回位和下一层解除 20% 限速。
原厂的 9.3 mm 恢复挤出量也作为可配置参数保存。

状态文件在原厂 `file_path` 基础上增加大小与 SHA-256；相同路径但内容改变会在
任何运动前使 EEPROM 记录失效。记录偏移必须严格落在 UTF-8 G-Code 行边界，缺少
XYZ 或有效热端目标也会拒绝。含 `Tn` 的多工具文件在独立 485 工具板协议完成前
拒绝恢复。`virtual_sdcard` 的启动钩子若失败，会在执行第一条续打 G-Code 前停止并
进入 error，而不是绕过恢复继续打印。

`enabled: False`、`automatic_restore: False` 和
`z_restore_strategy: disabled` 仍是 F009 默认值。源码只在开发者显式选择
`assume_unchanged` 后才会执行，因为该策略假定断电期间 Z 丝杆没有滑移；这一物理
前提、回位轨迹和温控顺序必须在目标机验收后才能开放生产使用。
