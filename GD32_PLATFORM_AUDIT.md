# GD32 `lib` / `src` 平台审计

审计日期：2026-09-01

## 目录职责

`lib/gd32f30x/include` 和 `lib/gd32e23x/include` 当前提供 CMSIS 芯片定义及厂商外设寄存器宏。Klipper 平台实现位于 `src/gd32`，直接操作寄存器；没有必要为了“目录完整”引入整套厂商外设 `.c` 文件，否则会形成第二套时钟、GPIO、中断和超时模型。

原厂 `lib/gd32f30x/system_gd32f30x.c` 未加入 Makefile，且包含另一套重复的时钟初始化和多处无限等待。该文件已经删除，避免维护者误以为它参与最终固件。CMSIS 声明文件 `lib/gd32f30x/include/system_gd32f30x.h` 保留；实际启动、120 MHz 时钟、向量表和故障策略统一由 `src/gd32/gd32f30x.c` 管理。

平台层采用“唯一实现”原则：厂商头文件只提供寄存器、位定义及芯片常量，不在 `lib` 中保留另一套会与 Klipper 平台代码竞争的 GPIO、时钟、中断或外设初始化实现。

2026-09-01 进一步统一了 Klipper/Katapult 的目录边界：公共 GD32 运行时代码保持在一个扁平的 `src/gd32` 中，芯片家族差异使用文件名前缀表达；厂商头文件全部下沉到各自 `include/`。该调整只改变源码组织和包含路径，Klipper 的 F303 串口/USB/CAN 与 E230 两组串口构建矩阵均已回归通过。

## 本轮已修复

1. **E230 APB 时钟**：芯片初始化把 APB1/APB2 配为 72 MHz，但公共宏原先写成 36 MHz。已改为 72 MHz，修正硬件 SPI/I²C 的分频计算。
2. **E230x6 条件覆盖**：串口和 ADC 原先只进入 `CONFIG_MACH_GD32E230X8` 分支。已统一改用系列级 `CONFIG_MACH_GD32E23X`；x6/x8 共用的外设代码不再漏编。
3. **F303 容量变体**：xB/xC 使用 `GD32F30X_HD`，xE 改为 `GD32F30X_XD`，避免 512 KiB 变体沿用错误的中断和外设条件。
4. **ADC 启动延时**：删除可能被编译器优化掉的空循环，改用 Klipper 时钟的 10 us 确定延时。
5. **ADC 校准失控**：校准复位和校准过程增加 5 ms 硬超时，异常时进入 Klipper shutdown，不再永久卡死 MCU。
6. **内部温度通道**：TSVREN 改为按位置位，不再用整寄存器赋值覆盖其它 ADC 控制位。
7. **E230 延时接口**：TIMER2 后端补齐 `udelay()`，ADC 和其它底层外设不再依赖不存在的 Cortex-M3/M4 DWT 延时实现。
8. **GPIO/定时器**：GPIO 毛刺、端口校验、E230 高位输出读取、F303 AFIO/SWD 保护及 E230 TIMER2 比较调度已在对应移植文档中记录。
9. **启动代码清理**：删除未参与构建的厂商 `system_gd32f30x.c`，同时清除 F303/E230 平台入口中未使用的 CanBoot、IRQ、GPIO、LED 和时钟选择残留；E230 不再无理由提前开启 GPIOA 时钟。
10. **原厂元数据覆盖代码**：旧 CRC 工具在链接后无条件改写应用偏移 `0x20C..0x211`，曾实际破坏跨过该区间的 `USART0_IRQHandler`。现由链接脚本在 `0x200` 固定保留 18 字节 `.gd32_app_metadata`，并断言向量表和段尺寸；普通代码从元数据后开始。CRC 工具增加最小长度、读写错误检查及重复执行安全性，非 Katapult 构建自动做原厂格式验证。

## 当前功能完整度

| 模块 | F303 | E230 | 状态说明 |
|---|---|---|---|
| 启动/主时钟 | 120 MHz HXTAL+PLL | 72 MHz HXTAL+PLL | 能构建；均假定外部晶振为 8 MHz |
| GPIO | 已实现 | 已实现 | 需按实际封装核对可用引脚 |
| Klipper 调度时钟 | DWT+SysTick | TIMER2 CH0 + 16/32 位扩展 | F303 已修复 SysTick `LOAD` 被过早清零；E230 需重点压力测试 |
| UART | PA2/PA3、PA9/PA10 | PA2/PA3、PA9/PA10 | F303 USART0 已以 230400 8N1 实测 F7 收发；通用串口仍需压力测试 |
| ADC | ADC0/内部温度 | ADC/外部通道 | F303 PA0 已完成数字方波与公版 ADC/PRTouch ADC 实测；E230 内部温度尚未对外枚举 |
| SPI | SPI0/SPI1 | SPI0/SPI1 | 完整轮询和超时；封装可能未引出 SPI1 |
| I²C | I2C0/重映射/I2C1 | I2C0 | 有超时及总线恢复；必须实测 1/2/N 字节读取 |
| USB CDC | 经典 USBD 控制器实现 | 芯片无原生 USB | F303 双缓冲已通过 100 次 COM 重开/1700 请求及 PRTouch 共存回归；F009 实板仍待验 |
| 看门狗 | FWDGT 约 500 ms | FWDGT 约 500 ms | 能喂狗；尚未上报复位原因 |
| 芯片 UID | F303 USB 序列号 | 未使用 | F303 UID 地址需要实机核对 |
| 镜像元数据/Bootloader布局 | 8 KiB Katapult 与原厂 12 KiB 均可构建 | 原厂版本字段已恢复 | 原厂布局 CRC/长度已自动验证；Katapult Flash 后端仍需目标板实测 |
| CAN | CAN0 已接入 Klipper CAN | 不适用当前目标 | 支持三组映射；需要外部 CAN 收发器及实机验证 |
| DMA/硬件 PWM | 未接入 | 未接入 | 当前 Klipper 基本运动控制不依赖它们 |

### PRTouch V2 官方 MCU 源码与片上 ADC（2026-09-03）

已从创想三维官方仓库固定提交中逐字恢复 `src/prtouch_v2.c`，并保留官方原文件名；
归一化换行后的 SHA-256 为
`04e2fa4a50f80461bd97cf802893295a4ef0862b126e40e6c9e349769e3570d4`。官方实现通过私有
`sched.c` 在 idle 循环直接调用 `prtouch_task()`。当前公版树已恢复同一调度位置，
并删除早期 `DECL_TASK` 桥，避免改变原厂时序或形成双重调度。

V2 与 V3 MCU 协议已做成互斥构建选项。含 V2、PA0 片上 ADC、USB 双缓冲的 CCT6
镜像已完成 T113 干净构建，BIN 长度 49516、CRC16 `0x8424`、SHA-256
`64f276fffbd4ae67db207d77974bf5793af564100266f2b0120ef3da366671c1`。K1 的
GD32E230x8、12 KiB Bootloader 偏移、USART0 PA9/PA10 目标也已由 V57 与公版两棵树
分别构建，并通过 V1/V2 的 12/12 命令、9/9 响应合同。DL16 的 PWM0→D0→PA0 链路已
测得 100 kHz/37%、0 毛刺，但 E230 四通道压力板仍缺实物验证。详情见
`PRTouch_V2_MCU官方源码移植与CCT6实测.md`。

## 建议继续修改的项目

### P0：上板前必须处理或验证

- 用示波器确认 F303 120 MHz、E230 72 MHz，以及 APB、UART、SPI、I²C 实际频率。
- 对 E230 TIMER2 做长时间时钟同步、16 位回绕、高步频和高中断负载测试。
- F303 PA0 单通道与校准已通过方波实测；仍需多个 ADC 引脚交替采样、开路/短路和热敏电阻实际误差。
- 用逻辑分析仪覆盖 I²C 的 1、2、3 及长数据读取、重复起始、NACK 和 SDA 锁死恢复。
- F303 USB 已验证 PA11/PA12 枚举、单缓冲和双缓冲消息通信、100 次快速重连及 USART0 并发；仍需长时间吞吐、物理热插拔、挂起/恢复和异常复位压力测试。

### P1：建议代码补全

- 把 8 MHz HXTAL 作为明确的 Kconfig/板级参数；晶振失败时进入可诊断 shutdown 或安全回退，替代启动阶段无限循环。
- 串口增加 ORE、FE、NE 等错误处理和计数，避免线路干扰后接收状态不透明。
- ADC 改成一次性外设初始化、每通道只做 GPIO 设置，避免每注册一个传感器都重新校准整个 ADC。
- 增加复位原因读取与上报，区分上电、软件复位、独立看门狗和欠压复位。
- 将看门狗超时做成平台常量并记录实测 IRC40K 偏差。
- 将 `usbfs.c`/`usbfs_regs.h` 重命名为 `usbd.c`/`usbd_regs.h`。当前 F303xB/xC/xE 路径使用的是经典 PMA USBD 控制器，不是 F30x CL 变体的 OTG USBFS；现有名字容易误导维护者。
- 公版 USB CDC 的 192 字节发送队列在高频 48 字节异步 ADC 报告下会产生明显背压，且 `console_sendf()` 满队列时没有控制响应优先级。单/双缓冲 A/B 现象一致；自定义优化应在兼容开关下增加生产者限速、水位统计或控制响应优先级，不能冒充 GD32 双缓冲修复。

### P2：按项目需求增加

- 若采用原厂 Bootloader，增加明确的原厂升级入口/复位握手；通用 `armcm_reset.c` 只识别 CanBoot 签名，不能据此假设能进入创想 Bootloader。
- 需要 CAN、DMA、硬件 PWM、Flash 参数区时，再分别增加 Klipper 平台后端；不要仅复制厂商库文件就宣称支持。
- 如果需要 E230x6，必须提供裁剪配置。x6 只有 32 KiB Flash/6 KiB RAM，F8 的全功能验证配置不可能原样使用。

## `lib` 是否需要补全

目前不建议整包补齐厂商库源码。应补充的是：

- 与所选完整料号和芯片修订匹配的官方数据手册、参考手册、勘误和启动文件版本记录；
- 对实际使用寄存器的缺失定义；
- 来源、版本和许可证清单；
- 编译期静态断言或目标矩阵，防止不同容量/系列误用宏。

只有准备实现 CAN、Flash 写入、DMA 等新后端时，才按模块引入必要定义，并继续由 `src/gd32` 维护符合 Klipper 语义的实现。
